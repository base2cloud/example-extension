# Example Chrome Extension Publish to Chrome Web Store

This repository is PoC example publishing chrome extension to Chrome Web Store(CWS)
by assuming STS credentials. It enables the audit logs for the STS which will be used
to verify a CWS publish.