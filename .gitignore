secrets.txt
*.zip

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# terraform.tfvars is committed on purpose: it holds only the project id and
# region. Anything sensitive goes into Secret Manager, never a tfvars file.
